`.env`
```sh
RESEND_API_KEY="Get one from resend.com"

OPAQUE_SERVER_SETUP="npx @serenity-kit/opaque create-server-setup"

DATABASE_URL="Create Postgres database on neon.com"

SERVER_HOST="http://127.0.0.1:8080" # local development
SERVER_HOST="URL of your deployment" # production

TURNSTILE_SITEKEY="Get one from cloudflare.com"
TURNSTILE_SECRET="Get one from cloudflare.com"

UPSTASH_REDIS_REST_URL="Get one from up upstash.com"
UPSTASH_REDIS_REST_TOKEN="Get one from upstash.com"

IS_DEV="true" # local development only
```
